## 윤강
## 총점: 42 / 76점
## 100점 환산: 약 55.3점

# Python Chapter 01~19 오답노트

---

## 1번

**정답:** 컴퓨터가 해야 할 일과 순서를 명확하게 설명하는 과정

✅ 정답 처리

---

## 2번

**정답:** 1, 2, 4

✅ 정답

---

## 3번

**정답:** Python 실행기(인터프리터)

✅ 정답

---

## 4번

**정답:** 1, 2, 4

❌ 오답

### 오답노트

`python --version`이 정상적으로 실행되지 않을 때는 다음을 확인할 수 있다.

* 현재 터미널의 오류 메시지 확인
* Windows에서는 `py --version` 확인
* Python 설치 후 새로운 터미널을 열어 다시 확인

**핵심:** 개발환경에 문제가 생기면 오류 메시지, Python 실행기, 터미널 상태를 차례대로 확인한다.

---

## 5번

**정답:** 오류 메시지

✅ 정답

---

## 6번

**정답:** 1, 2, 3, 4

✅ 정답

---

## 7번

**정답:** Python 100

✅ 정답 처리

---

## 8번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 9번

**정답:** `str`

✅ 정답 처리

---

## 10번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

```python
int("25")
```

문자열 `"25"`는 숫자로 바꿀 수 있기 때문에 정수 `25`가 된다.

```python
type(int("25"))
```

결과는 `int`이다.

**핵심:** 숫자 모양의 문자열은 `int()`로 정수 변환이 가능하다.

---

## 11번

**정답:** 3, 2

❌ 오답

### 오답노트

```python
17 // 5
```

결과는 `3`

```python
17 % 5
```

결과는 `2`

* `//` : 몫
* `%` : 나머지

**핵심:** `17 = 5 × 3 + 2`

---

## 12번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

```python
2 ** 3   # 8
10 / 2   # 5.0
17 % 5   # 2
10 != 7  # True
```

`3 > 5`만 `False`이다.

**핵심:** `/`는 나눗셈, `%`는 나머지, `!=`는 같지 않다는 뜻이다.

---

## 13번

**정답:** `yth`

❌ 오답

### 오답노트

```python
text = "Python"
print(text[1:4])
```

인덱스는 다음과 같다.

```text
P y t h o n
0 1 2 3 4 5
```

`[1:4]`는 1번부터 4번 **직전까지** 가져온다.

따라서:

```text
yth
```

**핵심:** 슬라이싱의 마지막 번호는 포함하지 않는다.

---

## 14번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

문자열에서 사용할 수 있는 대표 기능:

```python
strip()
replace()
upper()
len()
```

`append()`는 문자열이 아니라 **리스트**에서 사용하는 메서드이다.

---

## 15번

**정답:** `str`

✅ 정답 처리

---

## 16번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

`input()`의 기본 결과는 항상 문자열이다.

```python
age = input()
```

숫자 계산이 필요하면 변환한다.

```python
age = int(input())
```

또한 입력값의 앞뒤 공백은 다음처럼 제거할 수 있다.

```python
name = input().strip()
```

**핵심:** `input()` → 기본 자료형은 `str`

---

## 17번

**정답:** 통과

✅ 정답

---

## 18번

**정답:** 1, 2, 4, 5

❌ 오답

### 오답노트

```python
if 조건:
    실행할 코드
```

조건이 `True`이면 들여쓰기된 코드가 실행된다.

또한:

```python
=    # 값 대입
==   # 값 비교
```

두 연산자는 역할이 다르다.

---

## 19번

**정답:** 5번

✅ 정답

---

## 20번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 21번

**정답:** `fruits[-1]`

❌ 오답

### 오답노트

```python
fruits = ["사과", "바나나", "포도"]
```

마지막 값을 가져오려면:

```python
fruits[-1]
```

결과:

```text
포도
```

**핵심:** 마지막 요소의 인덱스는 `-1`

---

## 22번

**정답:** 1, 2, 4, 5

❌ 오답

### 오답노트

리스트에서는 다음 기능을 사용할 수 있다.

```python
append()
sum()
sort()
```

또한 리스트의 특정 값을 수정할 수도 있다.

```python
numbers[0] = 100
```

**핵심:** 리스트의 첫 번째 인덱스는 `1`이 아니라 `0`

---

## 23번

**정답:** `,`

✅ 정답

---

## 24번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

집합은 중복 제거뿐 아니라 집합 연산에도 사용할 수 있다.

```python
a | b   # 합집합
a & b   # 교집합
a - b   # 차집합
```

빈 집합은:

```python
set()
```

---

## 25번

**정답:** `student["score"]`

❌ 오답

### 오답노트

```python
student = {
    "name": "민수",
    "score": 85
}
```

점수 값을 가져오려면:

```python
student["score"]
```

결과:

```text
85
```

**핵심:** 딕셔너리는 인덱스가 아니라 **키**를 이용해 값을 조회한다.

---

## 26번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

딕셔너리에서 자주 사용하는 기능:

```python
student.get("name")
student.items()
"name" in student
```

* `get()` : 키를 안전하게 조회
* `items()` : 키와 값을 함께 가져오기
* `in` : 키 존재 여부 확인

---

## 27번

**정답:** `return`

✅ 정답

---

## 28번

**정답:** 1, 3, 5

✅ 정답

---

## 29번

**정답:** `venv` 또는 가상환경

✅ 정답 처리

---

## 30번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 31번

**정답:** `r`

❌ 오답

### 오답노트

파일 읽기 모드는:

```python
open("memo.txt", "r")
```

파일 모드:

```text
r = read = 읽기
w = write = 쓰기
a = append = 추가
```

---

## 32번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

CSV에서 읽은 숫자는 문자열일 수 있다.

계산해야 한다면:

```python
int()
```

또는:

```python
float()
```

로 변환한다.

---

## 33번

**정답:** `ValueError`

❌ 오답

### 오답노트

```python
int("abc")
```

`"abc"`는 정수로 변환할 수 없기 때문에:

```text
ValueError
```

가 발생한다.

비교:

```python
"10" + 5
```

→ `TypeError`

```python
int("abc")
```

→ `ValueError`

**핵심:** 변환하려는 값 자체가 잘못되어 있으면 `ValueError`

---

## 34번

**정답:** 1, 3, 4, 5

❌ 오답

### 오답노트

트레이스백에서는 마지막 줄의 **예외 이름**을 확인하는 것이 중요하다.

여러 종류의 예외를 따로 처리할 수도 있다.

```python
try:
    pass
except ValueError:
    pass
except ZeroDivisionError:
    pass
finally:
    pass
```

`finally`는 오류 여부와 관계없이 마지막 정리 작업 등에 사용할 수 있다.

---

## 35번

**정답:** 객체(Object)

✅ 정답

---

## 36번

**정답:** 1, 3, 4

❌ 오답

### 오답노트

클래스에서:

```python
self
```

는 **현재 객체 자신**을 의미한다.

```python
__init__()
```

은 객체 생성 시 초기값을 저장하는 데 사용한다.

**핵심:**

* 클래스 → 설계도
* 객체 → 실제 만든 값
* `self` → 현재 객체
* `__init__()` → 초기값 저장

---

## 37번

**정답:** IPO

✅ 정답

---

## 38번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

프로그램을 만들기 전에 먼저 설계한다.

* 목적 정하기
* 범위 정하기
* 이번에 하지 않을 기능 정하기
* 데이터 구조 생각하기
* 작은 테스트 데이터의 예상 결과 만들기

**핵심:** 바로 코드를 만드는 것이 아니라 먼저 요구사항과 설계를 정한다.

---

# B형

## 39번

**정답:** 읽고, 실행하고, 수정하고, 검증하는 능력

✅ 정답 처리

---

## 40번

**정답:** 1, 2, 4

✅ 정답

---

## 41번

**정답:** `python --version`

✅ 정답

---

## 42번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

개발환경 구성 요소:

* VS Code → 코드 작성과 파일 관리
* Python 실행기 → `.py` 실행
* Python 확장 → VS Code와 Python 연결
* Terminal → 명령 실행과 결과 확인

Explorer는 Python 패키지를 자동 설치하는 기능이 아니다.

---

## 43번

**정답:** 비밀번호 / API 키 / 개인정보

✅ 정답

---

## 44번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 45번

**정답:** `#`

✅ 정답

---

## 46번

**정답:** 1, 2, 5

✅ 정답

---

## 47번

**정답:** `ValueError`

✅ 정답

---

## 48번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

```python
int("25")      # int
float("3.5")   # float
str(100)       # str
bool(0)        # bool
```

하지만:

```python
int("hello")
```

는 정수로 변환할 수 없어 `ValueError`가 발생한다.

---

## 49번

**정답:** 9

✅ 정답

---

## 50번

**정답:** 1, 3, 4

❌ 오답

### 오답노트

```python
3 > 7 or 10 == 10
```

계산하면:

```text
False or True
```

따라서 결과는:

```text
True
```

**핵심:**

* `and` → 둘 다 True
* `or` → 하나라도 True
* `not` → True/False 반대로

---

## 51번

**정답:** `n`

✅ 정답

---

## 52번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

문자열은 한 번 만들어지면 특정 위치의 글자를 직접 바꿀 수 없다.

```python
word = "Python"
word[0] = "J"
```

이런 식의 변경은 불가능하다.

**핵심:** 문자열은 직접 수정할 수 없는 자료형이다.

---

## 53번

**정답:** 13

✅ 정답

---

## 54번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

`input()`을 사용할 때 기억할 것:

```text
input() → str
```

숫자가 필요하면:

```python
int(input())
```

공백 제거:

```python
input().strip()
```

잘못된 숫자 변환:

```python
int("abc")
```

→ `ValueError`

---

## 55번

**정답:** B

✅ 정답

---

## 56번

**정답:** 1, 2, 4, 5

❌ 오답

### 오답노트

조건문에서는 **경계값 테스트**가 중요하다.

예:

```python
if score >= 80:
```

이라면 다음 값을 확인해본다.

```text
79
80
81
```

또 조건이 겹치는 경우에는 `if`, `elif`의 순서도 중요하다.

---

## 57번

**정답:** 6

❌ 오답

### 오답노트

```python
total = 0

for n in range(1, 4):
    total += n
```

`range(1, 4)`는:

```text
1, 2, 3
```

따라서:

```text
1 + 2 + 3 = 6
```

---

## 58번

**정답:** 1, 2, 4, 5

❌ 오답

### 오답노트

`for`문은 여러 값을 하나씩 꺼내 처리할 수 있다.

```python
for item in items:
    print(item)
```

`while`문에서 조건에 사용되는 값이 변하지 않으면 무한 반복이 될 수 있다.

---

## 59번

**정답:** `[1, 2, 3]`

✅ 정답

---

## 60번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

리스트에서 사용할 수 있는 기능:

```python
len()
in
pop()
index()
```

하지만:

```python
keys()
```

는 리스트가 아니라 **딕셔너리**에서 사용하는 메서드이다.

---

## 61번

**정답:** `set()`

✅ 정답

---

## 62번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 63번

**정답:** `get()`

✅ 정답

---

## 64번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

딕셔너리에서:

```python
keys()
```

→ 키 확인

```python
values()
```

→ 값 확인

```python
items()
```

→ 키와 값을 함께 확인

**핵심:**

```text
keys → 키
values → 값
items → 키 + 값
```

---

## 65번

**정답:** 매개변수(parameter)

✅ 정답

---

## 66번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

함수 이름은 역할이 드러나게 작성하는 것이 좋다.

함수 안의 지역 변수는 일반적으로 함수 밖에서 바로 사용할 수 없다.

```python
def test():
    number = 10
```

또 함수에서는 기본값 매개변수와 키워드 인수를 사용할 수 있다.

```python
def greet(name="홍길동"):
    return name

greet(name="민수")
```

---

## 67번

**정답:** `requirements.txt`

✅ 정답

---

## 68번

**정답:** 1, 2, 3, 5

✅ 정답

---

## 69번

**정답:** `a`

❌ 오답

### 오답노트

기존 파일 뒤에 내용을 추가하려면:

```python
with open("result.txt", "a") as file:
    file.write("내용")
```

`a`는 `append`를 의미한다.

주의:

```python
append()
```

→ 리스트 메서드

```text
a
```

→ 파일 추가 모드

---

## 70번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

CSV 처리 시 확인할 것:

* 열 이름을 사용하려면 `DictReader` 고려
* 숫자 문자열은 계산 전에 형변환
* 파일 경로 확인
* 반복문으로 여러 행 처리 가능

`FileNotFoundError`가 발생하면 파일 경로를 반드시 확인해야 한다.

---

## 71번

**정답:** 예외 이름과 설명(오류 메시지)

✅ 정답

---

## 72번

**정답:** 1, 2, 3, 5

❌ 오답

### 오답노트

대표적인 실행 중 예외:

```text
ValueError
ZeroDivisionError
FileNotFoundError
KeyError
```

반면:

```text
SyntaxError
```

는 문법 자체가 잘못된 **문법 오류**이다.

---

## 73번

**정답:** `__init__()`

✅ 정답

---

## 74번

**정답:** 1, 2, 4, 5

✅ 정답

---

## 75번

**정답:** 함수

✅ 정답

---

## 76번

**정답:** 1, 2, 3, 4

❌ 오답

### 오답노트

프로그램을 작성하기 전에 먼저 다음을 생각한다.

1. 저장할 데이터의 모양
2. Input / Process / Output
3. 이번에 구현하지 않을 범위
4. 작은 테스트 데이터의 예상 결과

그 다음 코드를 작성하고 실제 결과와 예상 결과를 비교한다.

**핵심 흐름:**

```text
요구사항
↓
IPO 설계
↓
데이터 구조
↓
함수로 기능 분리
↓
작은 테스트
↓
코드 작성
↓
검증 및 수정
```

---

# 시험 전 핵심 암기

```text
// → 몫
%  → 나머지

슬라이싱의 끝 번호는 포함하지 않음

input() → str

int("abc") → ValueError

"10" + 5 → TypeError

리스트 마지막 값 → list[-1]

빈 집합 → set()

딕셔너리
get() → 안전한 조회
keys() → 키
values() → 값
items() → 키와 값

return → 함수 결과 반환

매개변수 → 함수 정의에서 값을 받는 이름

venv → 가상환경

파일 모드
r → 읽기
w → 쓰기
a → 기존 내용 뒤에 추가

트레이스백 마지막 줄
→ 예외 이름 + 오류 설명

클래스 → 설계도
객체 → 실제 만든 값
self → 현재 객체
__init__() → 초기값 저장

IPO
→ Input / Process / Output

큰 기능을 역할별 작은 코드 단위로 나누기
→ 함수
```
