[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-data-analysis-course"
version = "0.1.0"
description = "Utilities for the LLM Data Analysis Course"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["course_utils"]
